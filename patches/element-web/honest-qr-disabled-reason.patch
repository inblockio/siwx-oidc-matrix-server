diff --git a/apps/web/src/components/views/settings/devices/LoginWithQRSection.tsx b/apps/web/src/components/views/settings/devices/LoginWithQRSection.tsx
index f7f73b9..1b0f11a 100644
--- a/apps/web/src/components/views/settings/devices/LoginWithQRSection.tsx
+++ b/apps/web/src/components/views/settings/devices/LoginWithQRSection.tsx
@@ -21,6 +21,14 @@ import { useAsyncMemo } from "../../../../hooks/useAsyncMemo";
 interface IProps {
     onShowQr: () => void;
     isCrossSigningReady?: boolean;
+    /**
+     * Begin verification of the CURRENT session.
+     *
+     * Offered as the way out when device linking is blocked by this session not being
+     * cross-signed yet (see the render body). Optional, so the section still renders in
+     * contexts that have no such affordance to offer.
+     */
+    onVerifyCurrentDevice?: () => void;
 }
 
 export async function shouldShowQrForLinkNewDevice(cli: MatrixClient, isCrossSigningReady: boolean): Promise<boolean> {
@@ -29,7 +37,7 @@ export async function shouldShowQrForLinkNewDevice(cli: MatrixClient, isCrossSig
     return doesServerHaveSupport && !!cli.getCrypto()?.exportSecretsBundle && isCrossSigningReady;
 }
 
-const LoginWithQRSection: React.FC<IProps> = ({ onShowQr, isCrossSigningReady }) => {
+const LoginWithQRSection: React.FC<IProps> = ({ onShowQr, isCrossSigningReady, onVerifyCurrentDevice }) => {
     const cli = useMatrixClientContext();
     const offerShowQr = useAsyncMemo(
         () => shouldShowQrForLinkNewDevice(cli, !!isCrossSigningReady),
@@ -37,6 +45,22 @@ const LoginWithQRSection: React.FC<IProps> = ({ onShowQr, isCrossSigningReady })
         false,
     );
 
+    // The button is disabled by a conjunction (the server/provider supporting MSC4108 device
+    // link AND this session being cross-signed), but only one explanation was ever rendered:
+    // "Not supported by your account provider". When the failing conjunct is
+    // `isCrossSigningReady`, that sentence is FALSE - the account provider supports device link
+    // perfectly well; it is this session's own crypto state that is not ready. Worse, it is
+    // false in the one direction that matters: it tells the user the problem is not theirs to
+    // fix, while the fix is one click away.
+    //
+    // So: name the real blocker, and offer the action that clears it. This changes no gate and
+    // no cryptographic check - `offerShowQr` is computed exactly as before.
+    //
+    // `isCrossSigningReady` is `undefined` while SessionManagerTab's async probe is in flight;
+    // that transient case keeps the upstream wording (an upstream flash on every tab open, left
+    // untouched here so this patch stays about the steady state).
+    const blockedByThisSession = !offerShowQr && isCrossSigningReady === false;
+
     return (
         <SettingsSubsection heading={_t("settings|sessions|sign_in_with_qr")}>
             <div className="mx_LoginWithQRSection">
@@ -45,7 +69,25 @@ const LoginWithQRSection: React.FC<IProps> = ({ onShowQr, isCrossSigningReady })
                     <QrCodeIcon height={20} width={20} />
                     {_t("settings|sessions|sign_in_with_qr_button")}
                 </AccessibleButton>
-                {!offerShowQr && <Text size="sm">{_t("settings|sessions|sign_in_with_qr_unsupported")}</Text>}
+                {!offerShowQr &&
+                    (blockedByThisSession ? (
+                        <>
+                            <Text size="sm" data-testid="sign-in-with-qr-blocked-reason">
+                                {_t("settings|sessions|sign_in_with_qr_unverified_session")}
+                            </Text>
+                            {!!onVerifyCurrentDevice && (
+                                <AccessibleButton
+                                    kind="link"
+                                    onClick={onVerifyCurrentDevice}
+                                    data-testid="sign-in-with-qr-verify-session-button"
+                                >
+                                    {_t("settings|sessions|verify_session")}
+                                </AccessibleButton>
+                            )}
+                        </>
+                    ) : (
+                        <Text size="sm">{_t("settings|sessions|sign_in_with_qr_unsupported")}</Text>
+                    ))}
             </div>
         </SettingsSubsection>
     );
diff --git a/apps/web/src/components/views/settings/tabs/user/SessionManagerTab.tsx b/apps/web/src/components/views/settings/tabs/user/SessionManagerTab.tsx
index 01aba5c..19493ce 100644
--- a/apps/web/src/components/views/settings/tabs/user/SessionManagerTab.tsx
+++ b/apps/web/src/components/views/settings/tabs/user/SessionManagerTab.tsx
@@ -164,7 +164,12 @@ const SessionManagerTab: React.FC<{
     const currentUserMember = (userId && matrixClient?.getUser(userId)) || undefined;
     const isCrossSigningReady = useAsyncMemo(
         async () => matrixClient.getCrypto()?.isCrossSigningReady() ?? false,
-        [matrixClient],
+        // `devices` is a dependency so this re-probes after `refreshDevices()`, which is exactly
+        // what runs when the user finishes the verification the QR section now offers. Without
+        // it the probe happens once per mount, and a user who took the offered route would still
+        // be told to verify a session they had just verified - a second false statement replacing
+        // the first. The probe is a local crypto-store read, so re-running it is cheap.
+        [matrixClient, devices],
     );
 
     const onDeviceExpandToggle = (deviceId: ExtendedDevice["device_id"]): void => {
@@ -271,7 +276,11 @@ const SessionManagerTab: React.FC<{
     return (
         <SettingsTab>
             <SettingsSection>
-                <LoginWithQRSection onShowQr={onShowQrClicked} isCrossSigningReady={isCrossSigningReady} />
+                <LoginWithQRSection
+                    onShowQr={onShowQrClicked}
+                    isCrossSigningReady={isCrossSigningReady}
+                    onVerifyCurrentDevice={onVerifyCurrentDevice}
+                />
                 <SecurityRecommendations
                     devices={devices}
                     goToFilteredList={onGoToFilteredList}
diff --git a/apps/web/src/i18n/strings/en_EN.json b/apps/web/src/i18n/strings/en_EN.json
index 4ed51db..64938cb 100644
--- a/apps/web/src/i18n/strings/en_EN.json
+++ b/apps/web/src/i18n/strings/en_EN.json
@@ -2960,6 +2960,7 @@
             "sign_in_with_qr_button": "Show QR code",
             "sign_in_with_qr_description": "Use a QR code to sign in to another device and set up secure messaging.",
             "sign_in_with_qr_unsupported": "Not supported by your account provider",
+            "sign_in_with_qr_unverified_session": "This session isn't verified yet, so it can't set up another device. Verify this session first.",
             "sign_out": "Remove this session",
             "sign_out_all_other_sessions": "Remove all other sessions (%(otherSessionsCount)s)",
             "sign_out_confirm_description": {
