# Element X video-session establishment failure audit (2026-08-02)

**Status:** Audit complete, e2e-VERIFIED. Dev-staging fix (multi-homed ICE candidate leak) validated live 2026-08-02 ~22:54Z and re-verified off-box with the hardened `aqua-e2e` harness: **round 2 vs prod 13/13 PASS** (`~/.cache/aqua-e2e-logs/round2-prod-0115.log`), **round 3 vs dev-staging 13/13 PASS** (`round3-dev-1159.log`, 2026-08-02 ~10:00Z) — bidirectional call audio+video with 100% STT phrase coverage both directions, byte-exact E2EE messaging, well-known foci parity. During the green dev run LiveKit logged DTLS "error reading data channel" lines ONLY at the two call-teardown moments — the benign hangup signature — versus the establishment-window clusters of the broken state; use that distinction when reading logs. Prod carries the same defect but is unfixed — promotion is manual per policy (see addendum below).
**Scope:** repo `siwx-oidc-matrix-server` + companion repos (`aqua-agents` e2e harness) touched only for diagnosis; the code fix in this repo is `config/livekit.yaml` (`rtc.ips.excludes`), landed separately on `fix/av-ice-candidate-leak`.
**Origin:** overnight unsupervised session, 2026-08-01 evening through 2026-08-02 night. Gates for this session (live-box log inspection, dev-staging config change + restart, running real client calls) were pre-authorized by operator instruction; prod was intentionally left untouched (manual-promotion policy, see repo CLAUDE.md and the 2026-08-01 av-hardening plan).

## Goal

Explain why Element X (iOS) and browser video calls were failing to establish media, both in Tim's own 2026-08-01 16:42Z prod call and in the synthetic/real-client tests run across dev and prod during this session — and fix what could safely be fixed on dev-staging tonight.

## Hypothesis register

| ID | Hypothesis | Verdict | Evidence |
|----|------------|---------|----------|
| H1 | Multi-homed ICE candidate advertisement kills external media on dev: LiveKit is attached to two docker networks (compose-default + shared proxy net), STUN fails on the proxy-net interface, and LiveKit advertises the private proxy-net IP as an external ICE candidate. | **CONFIRMED.** Fix validated live 22:54Z. | Dev livekit log: `using external IPs {"ips": ["207.154.209.103/172.21.0.2", "172.18.0.5/172.18.0.5"]}` (two entries, second private). 12 DTLS timeouts in a real Element X (iPhone) + Chrome/Windows call attempt, 2026-08-01 22:03-22:07Z. After adding `rtc.ips.excludes: ["172.18.0.0/16"]` and restarting, dev log shows a single entry: `using external IPs {"ips": ["207.154.209.103/172.21.0.2"]}`. Harness (off-box, real-network) re-validation of the fix is still pending. |
| H2 | Prod has the same dual-advertisement defect. | **CONFIRMED**, fix not applied to prod. | Prod livekit log: `using external IPs {"ips": ["142.93.168.4/172.19.0.3", "172.18.0.4/172.18.0.4"]}` — same shape as dev, same shared-proxy-net (172.18.0.0/16) second entry. **And it is not latent:** the 240h DTLS-timeout histogram on prod clusters at 2026-08-01 16:42–16:43Z (18 lines — the establishment window of Tim's failed call, not its teardown), 22:05/22:07Z (the cross-env test) and 22:44Z (during the aqua-e2e run, where enough transports still won the candidate roulette for CALL-AV to pass). Prod deploys are manual per policy (repo CLAUDE.md); the fix goes to prod only via the promotion runbook below, on Tim's explicit decision. |
| H3 | The transport path for prod-homed calls (both peers provisioned against the prod homeserver) works correctly. | **CONFIRMED.** | 2026-07-31 84-minute browser call with full bidirectional audio/video. `aqua-e2e` CALL-AV check: bidirectional PASS, 2026-08-01 22:44Z. This rules out a systemic prod media-path break and localizes the incident to specific client/topology conditions (H4, H5). |
| H4 | Tim's 2026-08-01 16:42Z prod call failure was client-side: iOS Element X camera capture, not a server/transport defect. | **SUPPORTED but no longer exclusive** — H2's roulette is a live competing/compounding cause in the same window. | LiveKit session logs show 5 iOS join attempts in that window; audio was published twice, but no video track was ever created by the iOS client. The Android peer in the same window published both audio and video tracks normally. However, the prod DTLS-timeout histogram (see H2) shows 18 timeout lines during exactly this establishment window — a join landing on the dead private candidate publishes ZERO tracks (mic included), which with only 5 samples can mimic the "video-intent joins publish nothing, audio-intent joins work" correlation. The two causes may have compounded. Discriminator for the morning: after the prod ICE fix, retry a video call; if video still never publishes, run the phone-side checklist (camera permission, EX 26.08.0 update, rageshake). |
| H5 | Cross-homeserver test rooms silently split across two independent SFUs (prod-identity participants get the prod LiveKit focus, dev-identity participants get the dev focus), so a "mixed" call never actually shares a media server. | **CONFIRMED structurally.** | Live LiveKit sessions for the same nominal test call existed on BOTH SFUs simultaneously at 19:43Z and again at 22:03Z. MatrixRTC focus selection (MSC4143) is per-homeserver `rtc_foci`, so a dev-identity participant and a prod-identity participant in the same room each connect to their own homeserver's advertised focus with no bridging between the two SFUs. Any call test that mixes a dev-provisioned identity with a prod-provisioned identity is invalid as a media-path test until a focus-selection/bridging strategy is decided; single-homeserver tests (H1-H3) are unaffected. |
| H6 | The `aqua-e2e` harness self-poisoned its own crypto state during earlier failed runs, producing noise that looked like server-side call failures. | **CONFIRMED** from harness run logs. | A stable `device_id` combined with a wiped local store across restarts produced an OTK (one-time-key) collision, cascading into `InvalidSignature` Olm failures and a 300-second token-expiry cascade — all harness-side. This is the same failure class as the earlier "Transcript-agent OTK collision / Olm-deaf" incident in the aqua-agents fleet. Hardening for this lives in the `aqua-agents` branch `feat/aqua-e2e-hardening`, not in this repo. |
| H7 | The MatrixRTC discovery path (`.well-known` `rtc_foci`, the MSC4143 unstable-features flag, lk-jwt-service CORS) is intact on both prod and dev. | **CONFIRMED** by direct probes. | `.well-known/matrix/client` on both hosts advertises `org.matrix.msc4143.rtc_foci` with the correct `livekit_service_url`; lk-jwt-service responds with the expected CORS headers on both. Discovery was never the failure mode this session investigated — the defect is entirely in ICE candidate selection (H1/H2) and, separately, client-side capture (H4) and topology (H5). |

## Vendor-checklist deltas worth recording

- **Synapse 1.154 does not advertise `org.matrix.msc4143` in `unstable_features`.** The upstream fix lands in Synapse 1.156.0.
- **Synapse 1.157.0 removes `experimental_features.msc3861` entirely** (the MSC3861 delegated-auth config surface this stack depends on migrates to a different mechanism upstream). Effective upgrade ceiling for this stack is **1.156.x** until the auth migration is planned and executed. The dev `.env` image pin is already frozen for this reason — do not bump past 1.156.x casually.
- **No TURN server is deployed anywhere in this stack** (`turn.enabled: false` in every `livekit.yaml` variant). LiveKit's own operational guidance is that roughly 10-20% of real-world sessions need TURN/TLS-443 to traverse symmetric NAT or restrictive firewalls; without it, ICE-TCP on 7881 is the only fallback path and **must stay firewall-open** on every box running LiveKit. This is a known gap, not something this audit fixes.
- **`lk-jwt-service:0.5.0` is current upstream latest**; `/sfu/get` is the legacy-but-still-used route (the pinned digest and the access-allowlist behavior are covered by the 2026-08-01 av-hardening plan, not repeated here).

## Prod promotion addendum

Extends the prod promotion runbook in `docs/superpowers/plans/2026-08-01-av-hardening-config.md`. Prod deployment remains **manual and requires Tim's explicit decision** — nothing in this audit authorizes pushing the fix live.

1. `scp config/livekit.yaml deploy@142.93.168.4:/home/deploy/matrix/stack/.staging-livekit.yaml`, back up the current file first (`cp -p config/livekit.yaml config/livekit.yaml.bak-$TS`, same pattern as the existing runbook), then `cp .staging-livekit.yaml config/livekit.yaml`.
2. `docker compose restart livekit` (config-only change; no image pull needed, no other service restart required).
3. **Gate on the log line, not on "it restarted cleanly":** `docker logs <livekit-container> | grep 'using external IPs'` must show exactly **one** entry (the public IP). Two entries means the exclude didn't take (check the mounted file actually changed, and that the container picked up the new config).
4. Run the off-box `aqua-e2e` harness (now env-parameterizable per H6's hardening branch) as the media-path gate — this is what actually proves an external client gets real media, as opposed to an on-box synthetic test riding the private bridge and masking the bug (exactly what happened during earlier dev validation, see below).
5. Existing gates from the 2026-08-01 runbook still apply: the Twirp hairpin private-source-address check and the bare-`/livekit/sfu`-reaches-200 check.
6. **Rollback:** `cp config/livekit.yaml.bak-$TS config/livekit.yaml && docker compose restart livekit`.

## Dev-staging live-state note

Fix applied live on dev-aquafire 2026-08-02 ~22:54Z. Backup of the pre-fix config is at `/home/dev/matrix-staging/config/livekit.yaml.bak-20260802`. `ci-deploy.sh` on that box only converges images (pulls/restarts containers to the pinned `:dev` tag); it does not touch the bind-mounted `config/` directory, so this hand-applied fix persists across ordinary CI-driven deploys until the repo change on `fix/av-ice-candidate-leak` is merged and the box is re-synced from git.

## Why on-box validation missed this for so long

Synthetic and same-box test calls ride the private docker-bridge path end to end (both peers effectively reachable via the private network), so ICE happily picks the private candidate that happens to also work locally, and the second, would-be-broken candidate is never exercised. Only a real external client (Element X over a cellular/home network, or a genuinely off-box browser) forces ICE to choose between a reachable public candidate and an unreachable private one — which is exactly the condition that exposed H1 on 2026-08-01 22:03Z. See the addendum in `docs/superpowers/plans/2026-08-01-av-hardening-config.md` for how this connects to that plan's own PARTIAL validation gap.

## Boundary conditions

- Diagnosis-only session for prod; the only prod-facing action was reading logs over read-only SSH. No prod config, container, or firewall was touched.
- Dev-staging fix was applied live by the orchestrator during this session (outside this worktree); the repo-side code change lives on `fix/av-ice-candidate-leak` and has not been deployed from this branch — the live box already runs the equivalent change by hand.
- H5 (cross-homeserver focus splitting) is a structural finding, not something this audit resolves; it blocks trusting any future mixed dev/prod-identity call test until a focus-selection strategy is decided.
- H4 (iOS capture) needs a phone-side checklist to move from SUPPORTED to CONFIRMED; out of scope for a server-side audit.
